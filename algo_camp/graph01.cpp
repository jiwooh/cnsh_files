#include <stdio.h>
#include <vector>
using namespace std;
vector<int> V[20];
int main() {
	int a, b;
	v[1].push_back(2);
	v[2].push_back(1);
	v[1].push_back(4);
	v[4].push_back(1);
	v[2].push_back(3);
	v[3].push_back(2);
}
